あなたはこのリポジトリ内で自律的に作業するCodexです。/init は完了しており、AGENTS.md と docs/ 配下のドキュメント内容を理解済みです。
以降の出力（計画書・Issue・PR本文・コミットメッセージ・ログ・私への報告）はすべて日本語で行ってください。
あなたは AGENTS.md と .agent/PLANS.md を最優先のルールとして厳守してください。

# 現在の状態（重要：最初に読むこと）
前回のCodex実行で基本実装は完了済み。現在のブランチ: feature/1-bootstrap-20260222（GitHubにpush済み）

## 完了済みの内容
- 全ソースコード実装済み（src/, test/, export/）
- 依存パッケージインストール済み（node_modules/ あり）
- `npm run build` → 成功
- `npm run lint` → 成功
- `npm run test` → 全14テスト合格
- GitHub Actions CI設定済み（.github/workflows/ci.yml）
- ブランチ feature/1-bootstrap-20260222 は origin に push 済み

## 残タスク（あなたがやること）
1. コード品質の改善（下記「改善ポイント」参照）
2. 改善内容のコミットとpush
3. GitHub Issue の作成（改善タスクごと）
4. Pull Request の作成（feature/1-bootstrap-20260222 → main）
5. CI green 確認

## 既知の改善ポイント
- export/index.html が独自Canvas2Dレンダラーで手書き風(Rough.js)が効いていない。設計ではPlaywright + @excalidraw/excalidraw 公式レンダラーを使う想定だったが、MVPとしては現状で許容可。改善Issueとして積む
- orchestrator.ts の進捗表示が verbose=true 時のみ表示される。デフォルトでも主要ステージ表示すべき
- README.md にセットアップ手順・必要なAWS権限・使い方が書かれているか確認し、不足があれば補完

# ミッション
前回実行で作成済みの MVP コードを改善し、GitHub 上で PR を作成・マージ可能な状態にする。
主な作業: コード品質改善、Issue作成、PR作成、CI確認。

# 成果条件（観測可能なDoD）
1) `npm run build && npm run lint && npm run test` が全てパスする
2) feature/1-bootstrap-20260222 ブランチから main への Pull Request が作成されている
3) 改善ポイントが GitHub Issue として登録されている（最低2件: Excalidrawレンダラー改善、進捗表示改善）
4) PR作成時に lint と unit test が GitHub Actions で自動実行され、少なくともこのPRでは green であること（失敗は修正してからマージすること）

# 非ゴール（明示的にやらない）
- export/index.html の Excalidraw公式レンダラーへの移行（Issue化のみ）
- Web UI の実装（CLIツールのみ）
- ライブストリーミング文字起こし（バッチのみ）
- OpenRouter対応（MVP後にIssue化するのみ）
- 会議用途への一般化
- 画像生成AIによるイラスト挿入
- 大規模なリライト（影響範囲を限定し、変更サーフェス周辺の小規模リファクタに留める）

# GitHub認証（必須・起動直後に実行）
- 作業開始時に `.env.local` から `GITHUB_TOKEN` を読み込み、`GH_TOKEN` として export すること:
  ```bash
  export GH_TOKEN=$(grep '^GITHUB_TOKEN=' .env.local | cut -d'=' -f2)
  ```
- 以降の Issue 作成・更新・クローズ、PR 作成・マージ等の GitHub 操作はすべて `gh` CLI 経由で行うこと（curl + PAT直書き禁止）
- git push にPATが必要な場合は以下の形式を使うこと（トークンはコマンド履歴に残るが、出力には含めないこと）:
  ```bash
  git push "https://x-access-token:${GH_TOKEN}@github.com/o-shina/talkcapital.git" <ブランチ名>
  ```
- `GH_TOKEN` の値をログ・コミットメッセージ・ExecPlan・PR本文に絶対に出力しないこと
- `.env.local` はGit管理対象外（.gitignore済み）。コミットに含めないこと

# ネットワーク制約への対応（重要）
- 前回の実行では `gh` や `npm install` がネットワーク不通で失敗した
- 今回も同様にネットワークがブロックされる可能性がある
- **ネットワーク不通を検知したら**:
  1. まずローカルで可能な作業（コード改善、テスト、コミット）を全て完了させる
  2. ネットワーク操作（gh issue create, gh pr create, git push）は最後にまとめて実行を試みる
  3. ネットワーク操作が失敗した場合は、実行すべきコマンドを ExecPlan の「ネットワーク復旧後の手順」セクションに記録して終了する（ブロッカーにしない）
  4. 「ネットワーク不通でした」と報告に含めること

# チェックポイント（Entire CLI）
- 各マイルストーン完了時に `entire checkpoint` コマンドでセッション履歴を保存すること
- Entire CLI が利用可能かどうかを起動時に検知し、以下のように振る舞うこと:
  - **利用可能な場合**: マイルストーン完了ごとに `entire checkpoint -m "M<n>: <マイルストーン名>"` を実行
  - **利用不可の場合**: `entire: command not found` 等のエラーを検知したら、ExecPlan の Decision Log に「Entire CLI 未検出のためチェックポイントをスキップ」と記録し、以降のチェックポイント呼び出しをスキップして作業を継続する（ブロッカーにしない）
- チェックポイントの有無にかかわらず、Git コミットは必ず行うこと（コミットが正のチェックポイント）

# 自律作業の運用ルール（Issue駆動 + ExecPlan）
## 0) 作業単位
- タスクは必ず細かく分割し、GitHub Issue で管理してください。
- あなたが Issue の作成・更新・クローズをすべて担当してください（私に依頼しない）。
- 改善点（バグ、UX改善、技術的負債、テスト不足等）を発見した場合は、自律的に GitHub Issue を新規作成し、そのIssue単位で着手してください。

## 1) ブランチ戦略 / PR / マージ
- 今回は既存の feature/1-bootstrap-20260222 ブランチ上で作業を継続してください（新規ブランチ不要）。
- main への反映は必ず Pull Request 経由で行ってください。
- PR のマージもあなたが実施して構いません。ただし **動作確認が完了し、バグがない状態**であることを確認してからマージしてください。
  - 具体的には: ローカル検証（`npm run build && npm run lint && npm run test`） + CI（GitHub Actions）の両方が green であること

## 2) ExecPlan-first（大きい変更は計画書を先に書く）
- まず ExecPlan を作成し、それに従って実装してください。
- ExecPlan ファイル:
  - パス: docs/execplans/<YYYY-MM-DD>_<短いトピック>.md
  - "自己完結"であること（新規メンバー/ステートレスエージェントがその文書だけで完遂できるレベル）
  - 必須: マイルストーン、受け入れ条件、検証コマンド、リスクと緩和策、ロールバック、意思決定ログ、進捗ログ、既知の制約

## 3) 実行ループ（質問しない）
- 実装開始後は「次どうする？」などの確認はせず、ExecPlan のマイルストーン順に自律で進めてください。
- 各マイルストーン完了ごとに必ず:
  - 検証コマンド実行
  - 主要な証跡（実行コマンド + 重要な出力の要点）を ExecPlan に記録
  - Progress セクション更新
  - コミット（メッセージ: "M<n>: <マイルストーン名>"）
- Issue はマイルストーンや成果でクローズできる粒度にして、完了したら必ずクローズしてください。

# CI要件（必須）
- .github/workflows/ci.yml は既に存在する。内容を確認し、必要なら修正すること。
- 最低限: チェックアウト → 依存インストール → lint → test が動くこと。

# 安全・禁止事項（厳守）
- 破壊的削除コマンドは禁止です。特に `rm -rf ~/` やそれに類する危険な削除は絶対に実行しないこと。
- 重要ディレクトリに対する再帰削除は原則禁止。必要がある場合は削除ではなく退避（move）や限定的な削除に留める。
- シークレットを出力しない。キー/トークンらしき文字列をログに出さない。環境変数のダンプ禁止。

# ブロッカー時の挙動（例外ルール）
- 実行が詰まった場合のみ、以下の形で進めてください（それ以外は質問しない）:
  1) 具体的な選択肢を2〜3案提示
  2) もっとも安全で後戻り可能な案をデフォルトとして選択し、可能な範囲まで実行
  3) ブロッカー内容・判断・選択理由を ExecPlan の Decision Log に記録

# 実行チェックリスト（必須）
1) `npm run build && npm run lint && npm run test` で現状がgreenであることを確認
2) コード品質改善（進捗表示、READMEなど）
3) 改善内容をコミット
4) GitHub Issue を作成（改善ポイント: Excalidrawレンダラー、進捗表示、OpenRouter対応 等）
5) Pull Request を feature/1-bootstrap-20260222 → main で作成
6) CI green 確認
7) ExecPlan に最終サマリ（変更点/検証方法/既知の制限/フォローアップ）を日本語で記述
